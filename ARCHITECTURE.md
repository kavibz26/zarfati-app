# ארכיטקטורת מאגר המילים (Zarfati App)

מסמך זה מתעד את מבנה הנתונים של מאגר המילים נכון ל-2026-07-26: מקור האמת, איך מוסיפים/מעדכנים מילים, אילו קבצים הם תוצרי Build, ואילו חלקים מהתשתית הישנה עדיין קיימים ולמה.

## מקור האמת

**מקור האמת היחיד** למאגר המילים הוא שלושת הקבצים ב-`data/source/`:

- `data/source/words.csv` — כל מילת אוצר מילים (`id, es, he, ex_es, ex_he, levelId, topicId, topicName`).
- `data/source/sentences.csv` — כל משפט תרגול ל"בניית משפטים" (`id, es, he, levelId, topicId`).
- `data/source/levels-meta.json` — שם/אייקון/צבע לכל רמה (`beginner`/`intermediate`/`advanced`/`grammar`). לא כולל נושאים או מילים.

אלה קבצי הטקסט/CSV **היחידים** שמותר לערוך ידנית (או ב-Excel/Google Sheets). כל שאר קבצי הנתונים באפליקציה נגזרים מהם אוטומטית.

**מזהים (id):** לכל מילה/משפט יש מזהה קבוע ובלתי-תלוי בתוכן (`w000001`, `s000001`...). ברגע שהוקצה, המזהה נשאר צמוד לרשומה לנצח - גם אם משנים לה טקסט, נושא או מיקום בקובץ. **אין לגעת ב-id של שורה קיימת.** רק שורה חדשה עם `id` ריק מקבלת מזהה חדש אוטומטית בהרצת סקריפט היצירה (וגם נכתבת בחזרה ל-CSV כדי שהמזהה יישאר קבוע מעכשיו).

## איך מוסיפים או מעדכנים מילים

1. פותחים את `data/source/words.csv` (מילים חדשות) או `data/source/sentences.csv` (משפטי תרגול חדשים) בעורך טקסט/Excel/Sheets.
2. **מילה חדשה:** מוסיפים שורה עם `id` ריק, וממלאים `es, he, ex_es, ex_he, levelId, topicId`. אם זה נושא חדש שעדיין לא קיים ברמה, ממלאים גם `topicName`.
3. **עדכון מילה קיימת:** משנים את הטקסט הרצוי (es/he/ex_es/ex_he) **בלי לגעת ב-id**.
4. שומרים את ה-CSV, ומריצים:
   ```
   node scripts/generate-dictionary.js
   ```
5. הסקריפט יקצה מזהים לשורות החדשות (וישמור אותם בחזרה ל-CSV), ויבנה מחדש את כל קבצי הפלט (ראו למטה). אם `levelId` לא תקין, `topic`/`es`/`he` חסרים, או שיש מזהה כפול - הסקריפט יזרוק שגיאה מפורטת ולא יכתוב כלום.
6. בודקים באפליקציה (או מריצים `node scripts/verify-dictionary.js` כל עוד תשתית A עדיין קיימת - ראו למטה) שהכול תקין, ואז מבצעים commit לשינויים ב-`data/`.

**אין** דרך להוסיף/לערוך מילים על ידי עריכת קבצי JSON תחת `data/` ישירות - הם נדרסים בכל הרצה של `generate-dictionary.js`.

## סקריפטים

| סקריפט | מתי מריצים | מה הוא עושה |
|---|---|---|
| `scripts/generate-dictionary.js` | **בכל פעם שמעדכנים את ה-CSV** (התהליך הרגיל היחיד) | קורא את `data/source/*` וכותב מחדש את כל קבצי הפלט תחת `data/` |
| `scripts/csv-utils.js` | לא מריצים ישירות - מודול עזר (parse/write CSV) שבו משתמשים שני הסקריפטים למעלה/למטה | - |
| `scripts/verify-dictionary.js` | כלי בדיקה זמני, רלוונטי רק כל עוד תשתית A (`js/data/*.js`) עדיין קיימת | משווה מילה-במילה בין `js/data/*.js` (הישן) לבין `data/levels.json` + `data/words/*.json` (החדש) ומוודא 0 פערים. הופך למיותר ברגע שתשתית A תוסר |
| `scripts/export-legacy-to-csv.js` | **חד-פעמי, כבר הורץ** - אין להריץ שוב | ה-Bootstrap שיצר את `data/source/words.csv`/`sentences.csv` מתוך `js/data/*.js` בפעם הראשונה, עם הקצאת מזהים חדשים ויציבים |
| `scripts/test-id-stability.js` | מומלץ להריץ אחרי כל שינוי ב-`generate-dictionary.js` עצמו (לא נדרש אחרי עדכון CSV רגיל) | מריץ את `generate-dictionary.js` האמיתי על עותקי-ארגז-חול זמניים ומאמת בפועל: ייחודיות מזהים, עמידות לסדר שורות, עמידות לעריכת תוכן/נושא, והקצאת מזהה חדש רק לשורה חדשה |
| `scripts/test-lazy-loader.js` | מומלץ להריץ אחרי כל שינוי ב-`js/dictionary-loader.js` | מדמה `fetch` מול קבצי `data/` המקומיים, טוען את המודול האמיתי, ומשווה מילה-במילה מול `js/data/*.js` (תשתית A) |

## קבצי Build - אסור לערוך ידנית

כל הקבצים הבאים תחת `data/` (חוץ מ-`data/source/`) הם **תוצרי build** - נמחקים ונכתבים מחדש בכל הרצה של `generate-dictionary.js`. כל עריכה ידנית שלהם תימחק בהרצה הבאה:

- `data/levels.json` - שלד קל (רמות + נושאים + ספירות), בלי תוכן מילים. זה מה ש-`js/dictionary-loader.js` טוען קודם.
- `data/words/<levelId>__<topicId>.json` - vocab + sentences מלאים, קובץ נפרד לכל נושא (31 קבצים כרגע). נטענים לפי דרישה על ידי `js/dictionary-loader.js`.
- `data/search-index.json` - אינדקס דחוס (id/es/he) לכל המילים, לחיפוש עתידי. לא נטען עדיין באפליקציה.
- `data/id-registry.json` - ספר-הנהלה נגזר (לא מקור אמת עצמאי) - לבדיקת תקינות/מעקב מזהים בלבד.

## איך האפליקציה טוענת את הנתונים היום

`js/app.js` בונה את המשתנה הגלובלי `LEVELS` בזמן ריצה, דרך `js/dictionary-loader.js` (`DictionaryLoader.loadLevel(levelId)` לכל 4 הרמות במקביל, עם caching פנימי). התוצאה בדיוק באותה צורה שהייתה קיימת קודם (`LEVELS[id] = {id, name, icon, color, topics: [{id, name, vocab, sentences}]}`), כך שכל שאר `app.js` ממשיך לעבוד ללא שינוי. עד שהטעינה מסתיימת, האפליקציה נשארת במסך "טעינה".

## תשתית A - מה עוד קיים ולמה

הקבצים הבאים **עדיין קיימים על הדיסק** אך **אינם נטענים על ידי `index.html` יותר** (הוסרו תגיות ה-`<script>` שלהם) - הם קיימים רק לצורך גיבוי/רולבק/השוואה, עד שיוסרו בשלב נפרד:

- `js/data/beginner.js`, `js/data/intermediate.js`, `js/data/advanced.js`, `js/data/grammar.js` - קבצי הרמה הישנים (מבנה A, מזהים מבוססי-מיקום).
- `js/data.js` - המאסף הישן (בנה את `LEVELS` מקבצי הרמה למעלה).
- `js/data/search-index.js` - אינדקס חיפוש ישן שנוצר מתוך A, מעולם לא היה טעון בפועל באפליקציה.
- `scripts/import-words.js`, `scripts/generate-search-index.js` - סקריפטי הוספה/אינדוקס של תשתית A. **אין להשתמש בהם עוד** - הם כותבים לקבצי A שכבר אינם מקור האמת.

## מתי בטוח להסיר את תשתית A

להסיר את כל הקבצים ברשימה למעלה (וגם את `scripts/verify-dictionary.js`, שהופך למיותר יחד איתם) רק לאחר שכל התנאים הבאים מתקיימים:

1. תשתית B (`data/` + `js/dictionary-loader.js`) רצה יציבה באתר החי (production) פרק זמן סביר, בלי שגיאות שדווחו.
2. בוצעה לפחות עוד סבב אחד של הוספת/עדכון מילים אמיתי דרך `data/source/*.csv` + `generate-dictionary.js`, ואומת שהוא עובד כצפוי מקצה לקצה (כולל באתר החי, לא רק מקומית).
3. אין יותר צורך להשוות בין A ל-B (כלומר `verify-dictionary.js` כבר לא נחוץ כרשת ביטחון).
4. קיבלת אישור מפורש להסרה - כמו כל שינוי מבני אחר בפרויקט הזה.

עד אז, שתי התשתיות ממשיכות "לדור בכפיפה אחת" על הדיסק (A לא פעילה, B פעילה) - זו בדיוק כוונת המעבר ההדרגתי.
